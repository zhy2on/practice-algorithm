#include <bits/stdc++.h>

using namespace std;

int dx[4] = {1, 0, -1, 0};
int dy[4] = {0, 1, 0, -1};

int main(void) {
	int t;
	cin >> t;
	for (int t_c = 0; t_c < t; ++t_c) {
		int n;
		cin >> n;

		cout << '#' << t_c + 1 << '\n';

		for (int i = 0; i < n; ++i) {
			for (int j = 0; j < n; ++j) {
			}
			cout << '\n';
		}
	}
}
